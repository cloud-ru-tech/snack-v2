---
title: ProgressBar
description: ProgressBar component — linear progress indicator (0–100%).
version: "1.0.0"
---

import {
  ExampleContainer,
  ExampleItem,
  ExampleRow,
} from '#astro/components/mdx';
import StorybookIframe from '#astro/components/astro/StorybookIframe.astro';
import { ProgressBar, APPEARANCE } from '@packages/progress-bar/src';

# ProgressBar

Линейный индикатор прогресса: примеры, использование и API.

## Live examples

### Sizes

<ExampleContainer>
  <ExampleRow gap="large" justifyContent="center">
    <ExampleItem label="XS">
      <div style={{ width: "200px" }}>
        <ProgressBar progress={60} size="xs" appearance="primary" />
      </div>
    </ExampleItem>
    <ExampleItem label="S">
      <div style={{ width: "200px" }}>
        <ProgressBar progress={60} size="s" appearance="primary" />
      </div>
    </ExampleItem>
  </ExampleRow>
</ExampleContainer>

### Appearance

<ExampleContainer>
  {Object.values(APPEARANCE).map(appearance => {
    return (
      <ExampleRow key={appearance} gap="large" justifyContent="center" flexWrap="wrap">
        <ExampleItem label={appearance}>
          <div style={{ width: "200px" }}>
            <ProgressBar progress={60} size="s" appearance={appearance} />
          </div>
        </ExampleItem>
      </ExampleRow>
    );
  })}
</ExampleContainer>

## Storybook

### Playground

<StorybookIframe storyId="components-progressbar-progressbar--playground" showControls={true} height="400px" />

### Visual Matrix

Для просмотра всех размеров и вариантов ProgressBar в виде матрицы используйте story **Visual Matrix** в Storybook.

<StorybookIframe storyId="components-progressbar-progressbar--visual-matrix" showControls={false} height="400px" />

## Usage

### Basic

```tsx
import { ProgressBar } from '@design-system/progress-bar';

export function Example() {
  return <ProgressBar progress={50} size="s" appearance="primary" />;
}
```

### With custom progress

```tsx
import { ProgressBar } from '@design-system/progress-bar';

export function Example() {
  return (
    <ProgressBar
      progress={75}
      size="s"
      appearance="green"
    />
  );
}
```

## Props

[//]: DOCUMENTATION_SECTION_START
[//]: THIS_SECTION_IS_AUTOGENERATED_PLEASE_DONT_EDIT_IT
### ProgressBarProps
| name | type | default value | description |
|------|------|---------------|-------------|
| progress* | `number` | - | Процент загрузки от 0 до 100 |
| size | enum ProgressBarSize: `"s"`, `"xs"` | xs | Размер |
| appearance | enum Appearance: `"neutral"`, `"primary"`, `"red"`, `"orange"`, `"yellow"`, `"green"`, `"blue"`, `"violet"`, `"pink"` | - | Внешний вид |
| className | `string` | - | CSS-класс |


[//]: DOCUMENTATION_SECTION_END

## Accessibility

- Компонент использует семантическую разметку для индикатора прогресса
- Рекомендуется сопровождать индикатор текстом или `aria-label` с текущим значением для пользователей скринридеров

## Best practices

1. **Указывайте прогресс явно** — Всегда передавайте `progress` в диапазоне 0–100
2. **Подбирайте appearance** — Используйте primary/neutral для общего контекста; цветные варианты — для статуса (успех, предупреждение и т.д.)
3. **Размер по контексту** — XS для компактных мест (таблицы, карточки), S для основных экранов
