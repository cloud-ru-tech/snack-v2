---
// Оборачиваем MarkdownContent и добавляем спрайт иконок в начало,
// чтобы <use href="#snack-uikit-..."> в примерах находил символы в том же DOM-дереве
import Default from '@astrojs/starlight/components/MarkdownContent.astro';

---

<Default>
  <slot />
</Default>
