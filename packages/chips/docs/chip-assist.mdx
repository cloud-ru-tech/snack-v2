---
title: ChipAssist
package: '@ds/chips'
description: Кликабельный чип-кнопка для inline-действия — с иконкой, лейблом, размерами s/m/l и состояниями disabled/loading.
order: 1
---

import { AssistSizes } from '../demos/examples/AssistSizes';
import AssistSizesSrc from '../demos/examples/AssistSizes.tsx?raw';
import { AssistWithIcon } from '../demos/examples/AssistWithIcon';
import AssistWithIconSrc from '../demos/examples/AssistWithIcon.tsx?raw';
import { AssistStates } from '../demos/examples/AssistStates';
import AssistStatesSrc from '../demos/examples/AssistStates.tsx?raw';
import { Example } from '#docs/components/Example';
import { PropsTable } from '#docs/components/PropsTable';
import { StorybookEmbed } from '#docs/components/StorybookEmbed';
import { FigmaEmbed } from '#docs/components/FigmaEmbed';
import { figmaNode } from '#docs/lib/figma';
import chipsDoc from './props.json';

# ChipAssist

Кликабельный чип-кнопка. Инициирует действие в контексте фильтров, тегов и быстрых команд. Лейбл обязателен, иконка опциональна.

## Когда использовать

- Для inline-действий рядом с контентом: добавить тег, применить быструю команду.
- Когда действие визуально должно восприниматься как чип в ряду фильтров, а не как классическая кнопка.

Когда **не** нужен ChipAssist:

- Для основного CTA экрана — используйте [`Button`](/components/button/button).
- Для переключения бинарного состояния — используйте [`ChipToggle`](/components/chips/chip-toggle).

## Анатомия

### Size (default `s`)

- `s` — для плотных тулбаров и строк фильтров.
- `m` — стандартный размер.
- `l` — для крупных форм.

### Icon

Опциональная иконка перед лейблом через проп `icon`. При `loading` иконка заменяется спиннером.

### Состояния

- `disabled` — отключает взаимодействие.
- `loading` — показывает спиннер вместо иконки и блокирует клик.

## Установка

```bash
pnpm add @ds/chips
```

```ts
import { ChipAssist } from '@ds/chips';
```

## Примеры использования

<Example title='Три размера в ряд' code={AssistSizesSrc}>
  <AssistSizes client:only='react' />
</Example>

<Example title='С иконкой' description='Иконка перед лейблом через проп icon' code={AssistWithIconSrc}>
  <AssistWithIcon client:only='react' />
</Example>

<Example title='Состояния' description='default, disabled и loading' code={AssistStatesSrc}>
  <AssistStates client:only='react' />
</Example>

## Props

<PropsTable data={chipsDoc.ChipAssist} />

## Storybook

<StorybookEmbed storyId='components-chips-chipassist--playground' height={360} />

## Figma

<FigmaEmbed node={figmaNode('chips', 'chip-assist')} height={420} title='ChipAssist в Figma' />

## Смотри также

- [ChipToggle](/components/chips/chip-toggle) — чип с состоянием выбора.
- [Button](/components/button/button) — основные кнопки действий.
