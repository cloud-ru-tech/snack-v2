---
title: ChipChoiceRow
package: '@ds/chips'
description: Строка из нескольких ChipChoice-фильтров с pinned-чипами и кнопками добавить/сбросить.
order: 3
---

import { ChoiceRowBasic } from '../demos/examples/ChoiceRowBasic'
import ChoiceRowBasicSrc from '../demos/examples/ChoiceRowBasic.tsx?raw'
import { ChoiceRowPinned } from '../demos/examples/ChoiceRowPinned'
import ChoiceRowPinnedSrc from '../demos/examples/ChoiceRowPinned.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'
import chipsDoc from './props.json'

# ChipChoiceRow

Контейнер, который собирает несколько `ChipChoice`-фильтров в одну строку и управляет их общим состоянием. Добавляет кнопку выбора фильтров и кнопку сброса.

## Когда использовать

- Для панели фильтров над таблицей или списком, где фильтров несколько и они меняются вместе.
- Когда часть фильтров должна быть закреплена (`pinned`), а остальные — добавляться пользователем по необходимости.

Когда **не** нужен ChipChoiceRow:

- Для одного фильтра — используйте нужный `ChipChoice.*` напрямую.
- Для действий, а не фильтрации — [`ChipAssist`](/components/chips/chip-assist).

## Анатомия

### Size (default `s`)

Размер применяется ко всем чипам строки: `s` / `m` / `l`.

### Filters

Проп `filters` — массив описаний фильтров. Каждый элемент задаёт:

- `id` — уникальный ключ фильтра в состоянии.
- `type` — тип чипа из `CHIP_CHOICE_TYPE`:
  - `Single` — одиночный выбор.
  - `Multiple` — множественный выбор.
  - `Date` — дата.
  - `DateRange` — период.
  - `Time` — время.
  - `Custom` — произвольный контент.
- `label` — подпись чипа.
- `options` — список значений (для `Single` / `Multiple`).

### Pinned

Фильтр с `pinned: true` всегда виден в строке и не убирается из неё. Остальные фильтры пользователь добавляет через кнопку добавления.

### Add / Clear buttons

- `showAddButton` (default `true`) — кнопка выбора дополнительных фильтров.
- `showClearButton` (default `true`) — кнопка сброса всех значений.

## Установка

```bash
pnpm add @ds/chips
```

```ts
import { ChipChoiceRow, CHIP_CHOICE_TYPE } from '@ds/chips'
```

`ChipChoiceRow` использует портал для выпадающих меню фильтров — оберните дерево в `PortalContextProvider` из `@ds/portal-context`.

## Примеры использования

<Example
  title='Строка фильтров'
  description='Single + Multiple + Date с кнопками добавления и сброса'
  code={ChoiceRowBasicSrc}
>
  <ChoiceRowBasic client:only='react' />
</Example>

<Example
  title='Закреплённый фильтр'
  description='pinned: true держит чип в строке, остальные добавляются по кнопке'
  code={ChoiceRowPinnedSrc}
>
  <ChoiceRowPinned client:only='react' />
</Example>

## Props

<PropsTable data={chipsDoc.ChipChoiceRow} />

## Storybook

<StorybookEmbed storyId='components-chips-chipchoicerow--playground' height={360} />

## Figma

<FigmaEmbed node={figmaNode('chips', 'chip-choice-row')} height={420} title='ChipChoiceRow в Figma' />

## Смотри также

- [ChipChoice.Single](/components/chips/chip-choice-single) — одиночный фильтр.
- [Chips](/components/chips) — обзор пакета.
