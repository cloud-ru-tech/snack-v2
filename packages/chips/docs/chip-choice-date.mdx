---
title: ChipChoice.Date
package: '@ds/chips'
description: Чип-фильтр с выбором даты через календарь — режимы date, date-time, month, year, размеры s/m/l и состояния disabled/loading.
order: 33
---

import { ChoiceDateBasic } from '../demos/examples/ChoiceDateBasic'
import ChoiceDateBasicSrc from '../demos/examples/ChoiceDateBasic.tsx?raw'
import { ChoiceDateTime } from '../demos/examples/ChoiceDateTime'
import ChoiceDateTimeSrc from '../demos/examples/ChoiceDateTime.tsx?raw'
import { ChoiceDateMonth } from '../demos/examples/ChoiceDateMonth'
import ChoiceDateMonthSrc from '../demos/examples/ChoiceDateMonth.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'
import chipsDoc from './props.json'

# ChipChoice.Date

Чип-фильтр с выбором даты. Открывает календарь в поповере, значение — объект `Date`. Поддерживает выбор даты, даты со временем, месяца и года через проп `mode`.

## Когда использовать

- Для фильтрации списка или таблицы по дате в ряду чипов-фильтров.
- Когда нужно выбрать конкретный день, месяц, год или дату со временем компактным контролом.

Когда **не** нужен ChipChoice.Date:

- Для выбора диапазона дат — используйте [`ChipChoice.DateRange`](/components/chips/chip-choice-date-range).
- Для выбора только времени без даты — используйте [`ChipChoice.Time`](/components/chips/chip-choice-time).
- Для выбора из произвольного списка значений — используйте [`ChipChoice.Single`](/components/chips/chip-choice).

## Анатомия

### Size (default `s`)

- `s` — для плотных тулбаров и строк фильтров.
- `m` — стандартный размер.
- `l` — для крупных форм.

### Mode

Режим выбора задаётся пропом `mode` (default `date`):

- `date` — выбор конкретного дня.
- `date-time` — выбор дня и времени; вместе с ним доступен `showSeconds` для отображения секунд.
- `month` — выбор месяца.
- `year` — выбор года.

Текст внутри чипа настраивается через `valueRender(value)` — функция получает текущий `Date` и возвращает узел для отображения.

### Состояния

- `disabled` — отключает взаимодействие.
- `loading` — показывает спиннер и блокирует открытие календаря.

## Установка

```bash
pnpm add @ds/chips
```

```ts
import { ChipChoice } from '@ds/chips'
```

## Примеры использования

<Example
  title='Выбор даты'
  description='Контролируемое значение через value и onChange'
  code={ChoiceDateBasicSrc}
>
  <ChoiceDateBasic client:only='react' />
</Example>

<Example
  title='Дата и время'
  description="mode='date-time' с отображением секунд"
  code={ChoiceDateTimeSrc}
>
  <ChoiceDateTime client:only='react' />
</Example>

<Example title='Выбор месяца' description="mode='month'" code={ChoiceDateMonthSrc}>
  <ChoiceDateMonth client:only='react' />
</Example>

## Props

<PropsTable data={chipsDoc.ChipChoiceDate} />

## Storybook

<StorybookEmbed storyId='components-chips-chipchoice-date--playground' height={360} />

## Figma

<FigmaEmbed node={figmaNode('chips', 'chip-choice-date')} height={420} title='ChipChoice.Date в Figma' />

## Смотри также

- [ChipChoice.DateRange](/components/chips/chip-choice-date-range) — выбор диапазона дат.
- [ChipChoice.Time](/components/chips/chip-choice-time) — выбор времени.
- [ChipChoice.Single](/components/chips/chip-choice) — выбор одного значения из списка.
