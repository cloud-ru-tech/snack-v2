---
title: PromoTag
package: '@ds/promo-tag'
description: Промо-тег — цветной лейбл для маркетинговых акцентов, выделения категорий и статусных ярлыков. Опционально кликабельный.
order: 0
---

import { PromoTagDemo } from '../demos/PromoTagDemo'
import { Basic } from '../demos/examples/Basic'
import BasicSrc from '../demos/examples/Basic.tsx?raw'
import { Colors } from '../demos/examples/Colors'
import ColorsSrc from '../demos/examples/Colors.tsx?raw'
import { Clickable } from '../demos/examples/Clickable'
import ClickableSrc from '../demos/examples/Clickable.tsx?raw'
import { Polymorphic } from '../demos/examples/Polymorphic'
import PolymorphicSrc from '../demos/examples/Polymorphic.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import promoTagDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# PromoTag

Компактный цветной тег для маркетинговых подписей (`NEW`, `SALE`, `BETA`), категорий и акцентных меток. По умолчанию — `<div>`; при `onClick` — `<button>`; при `as='a'` или `as={Link}` — ссылка с пробросом `href` / `to`.

## Демо
<PromoTagDemo client:visible />

## Когда использовать
- Пометка карточки товара/услуги — новинка, скидка, хит.
- Цветовая категоризация в списках (темы, теги статей).
- Статусная метка, которая **не** несёт функциональной семантики (для статусов берите `Status`).

Когда **не** нужен: интерактивные chip'ы с удалением — берите `Chip`/`Tag`; функциональные статусы — берите `Status`.

## Анатомия

### Appearance
Цветовая палитра: `primary` — брендовый акцент, `neutral` — без окраски; `red`, `orange`, `yellow`, `green`, `blue`, `violet`, `pink` — декоративные категории.

### Size
`xs` — плотные ленты/карточки; `s` — дефолт; `m` — для крупных промо-блоков.

### Role appearance (default `accent`)
Роль, в которой применяется выбранный `appearance`:

- `accent` — насыщенная заливка, для главных меток (новинка, sale).
- `decor` — мягкая декоративная подложка, для категоризации и тегов.

## Установка
```bash
pnpm add @ds/promo-tag
```

```ts
import { PromoTag } from '@ds/promo-tag'
```

## Примеры использования
<Example title='Базовый тег' code={BasicSrc}>
  <Basic client:visible />
</Example>

<Example title='Палитра' description='Основные цветовые appearance.' code={ColorsSrc}>
  <Colors client:visible />
</Example>

<Example
  title='Кликабельный тег'
  description='Передайте onClick — компонент отрендерится как <button>.'
  code={ClickableSrc}
>
  <Clickable client:visible />
</Example>

<Example
  title='Polymorphic'
  description='as=&apos;a&apos; с href или as={Link} с to — для react-router-dom и внешних ссылок.'
  code={PolymorphicSrc}
>
  <Polymorphic client:visible />
</Example>

## Props
<PropsTable data={promoTagDoc.PromoTag} />

## Storybook
<StorybookEmbed storyId='components-promotag--playground' height={240} />

## Figma
<FigmaEmbed node={figmaNode('promo-tag')} height={480} title='PromoTag в Figma (Snack UI Kit)' />
