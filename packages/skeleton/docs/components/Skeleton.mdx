---
title: Skeleton
description: Блок-плейсхолдер загрузки с настраиваемой шириной, высотой и скруглением.
version: '0.1.0'
---

import {
  ExampleContainer,
  ExampleRow,
  ExampleItem,
} from '#astro/components/mdx';
import StorybookIframe from '#astro/components/astro/StorybookIframe.astro';
import { Skeleton } from '../../src';

# Skeleton

Блок-плейсхолдер загрузки. При `loading={true}` отображается анимированный скелетон, при `loading={false}` — `children`. Размеры задаются через `width`, `height`, `borderRadius`. Подходит для картинок, карточек, аватаров.

## Live examples

### Basic

<ExampleContainer>
  <ExampleRow gap="large" justifyContent="flex-start">
    <ExampleItem label="Блок 200×24">
      <Skeleton loading width={200} height={24}>
        <span>Контент после загрузки</span>
      </Skeleton>
    </ExampleItem>
  </ExampleRow>
</ExampleContainer>

```tsx
import { Skeleton } from '@design-system/skeleton';

export function BasicExample() {
  return (
    <Skeleton loading width={200} height={24}>
      <span>Контент после загрузки</span>
    </Skeleton>
  );
}
```

### Разные формы

<ExampleContainer>
  <ExampleRow gap="large" justifyContent="flex-start">
    <ExampleItem label="Прямоугольник">
      <Skeleton loading width={120} height={80} borderRadius={8} ><div>Карточка контента</div></Skeleton>
    </ExampleItem>
    <ExampleItem label="Круг (аватар)">
      <Skeleton loading width={48} height={48} borderRadius="50%" ><img src="/avatar.jpg" alt="" /></Skeleton>
    </ExampleItem>
  </ExampleRow>
</ExampleContainer>

```tsx
import { Skeleton } from '@design-system/skeleton';

export function LoadingCard() {
  return (
    <Skeleton loading width={120} height={80} borderRadius={8}>
      <div>Карточка контента</div>
    </Skeleton>
  );
}

export function LoadingAvatar() {
  return (
    <Skeleton loading width={48} height={48} borderRadius="50%">
      <img src="/avatar.jpg" alt="" />
    </Skeleton>
  );
}
```

### Состояние контента

При `loading={false}` отображаются `children` вместо скелетона:

<ExampleContainer>
  <ExampleRow gap="large" justifyContent="flex-start">
    <ExampleItem label="loading=true">
      <Skeleton loading width={200} height={24}>
        <span>Контент</span>
      </Skeleton>
    </ExampleItem>
    <ExampleItem label="loading=false">
      <Skeleton loading={false} width={200} height={24}>
        <span>Контент после загрузки</span>
      </Skeleton>
    </ExampleItem>
  </ExampleRow>
</ExampleContainer>

```tsx
import { Skeleton } from '@design-system/skeleton';

export function Example() {
  return (
    <>
      <Skeleton loading width={200} height={24}>
        <span>Контент</span>
      </Skeleton>
      <Skeleton loading={false} width={200} height={24}>
        <span>Контент после загрузки</span>
      </Skeleton>
    </>
  );
}
```

## Storybook

### Playground

<StorybookIframe storyId="components-skeleton-skeleton--playground" showControls={true} height="400px" />

### Visual Matrix

Для просмотра вариантов Skeleton в виде матрицы используйте story **Visual Matrix** в Storybook.

<StorybookIframe storyId="components-skeleton-skeleton--visual-matrix" showControls={false} height="400px" />

## Props

[//]: DOCUMENTATION_SECTION_START
[//]: THIS_SECTION_IS_AUTOGENERATED_PLEASE_DONT_EDIT_IT
### SkeletonProps
| name | type | default value | description |
|------|------|---------------|-------------|
| loading | `boolean` | - | Флаг состояния загрузки. Если значение true, будет отрисован блок скелетона, если false - children. |
| width | `Width<string \| number>` | - | Ширина блока. Можно указать значение допустимое для CSSProperty.width (пример `'60%'`, `'400px'` и т.д) |
| height | `Height<string \| number>` | - | Высота блока. Можно указать значение допустимое для CSSProperty.height (пример `'60%'`, `'400px'` и т.д) |
| borderRadius | `BorderRadius<string \| number>` | - | Радиус скругления. Можно указать значение допустимое для CSSProperty.borderRadius (пример `'10px'`, `'50%'` и т.д) |
| className | `string` | - | CSS-класс |


[//]: DOCUMENTATION_SECTION_END

## Accessibility

- Скелетон — декоративный элемент; он не должен быть единственным индикатором загрузки для скринридеров.
- Рекомендуется комбинировать с текстом или `aria-live`, чтобы пользователи скринридеров знали о состоянии загрузки.

## Best practices

1. **Соответствие контенту** — ширина и высота скелетона должны примерно совпадать с размерами загружаемого контента.
2. **Используйте children** — передавайте `children` для корректного переключения между скелетоном и контентом.
3. **Контекст** — `loading` можно задавать через проп или через `SkeletonContextProvider` для вложенных компонентов.
