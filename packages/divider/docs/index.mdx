---
title: Divider
description: Разделитель контента с горизонтальной и вертикальной ориентацией и вариантами толщины линии.
version: '0.1.0'
---

import { Divider } from '../src';
import { ExampleContainer, ExampleItem, ExampleRow } from '#astro/components/mdx';
import Changelog from '#astro/components/astro/Changelog.astro';
import LlmLink from '#astro/components/astro/LlmLink.astro';
import StorybookIframe from '#astro/components/astro/StorybookIframe.astro';

# Divider

Divider — компонент для визуального разделения контента. 

**Version:** `{frontmatter.version}`

## Installation

```bash
npm install @design-system/divider
# or
yarn add @design-system/divider
# or
pnpm add @design-system/divider
```

## Changelog

<Changelog packageName="divider" />

<LlmLink component="divider" />

## Overview

Divider используется для отделения блоков и элементов в интерфейсе. Компонент поддерживает:

- **Две ориентации** — горизонтальная (по умолчанию) и вертикальная для разделения контента в ряд
- **Два варианта толщины** — regular (1px) и thin (0.5px) для разной визуальной нагрузки
- **Семантика и доступность** — `role="separator"` и `aria-orientation` для скринридеров
- **Поддержка data-test-id** — через WithSupportProps для автотестов

## Примеры

Для каждого примера ниже показан результат и код, который его создаёт.

### Горизонтальный (по умолчанию)

<ExampleContainer>
  <div style={{ width: '100%', maxWidth: 320 }}>
    <Divider />
  </div>
</ExampleContainer>

```tsx
import { Divider } from '@design-system/divider';

export function Example() {
  return <Divider />;
}
```

### Вертикальный разделитель

<ExampleContainer>
  <div style={{ display: 'flex', alignItems: 'stretch', height: '48px', gap: '16px' }}>
    <div style={{ display: 'flex', alignItems: 'center' }}>Left</div>
    <div style={{ width: '4px', minWidth: '1px', flexShrink: 0 }}>
      <Divider orientation="vertical" />
    </div>
    <div style={{ display: 'flex', alignItems: 'center' }}>Right</div>
  </div>
</ExampleContainer>

```tsx
import { Divider } from '@design-system/divider';

export function Toolbar() {
  return (
    <div style={{ display: 'flex', alignItems: 'stretch', height: 48, gap: 16 }}>
      <span style={{ display: 'flex', alignItems: 'center' }}>Left</span>
      <div style={{ width: 4, minWidth: 1, flexShrink: 0 }}>
        <Divider orientation="vertical" />
      </div>
      <span style={{ display: 'flex', alignItems: 'center' }}>Right</span>
    </div>
  );
}
```

### Варианты толщины

<ExampleContainer>
  <ExampleRow gap="xlarge">
    <ExampleItem label="Regular (1px)" gap="medium" labelSize="medium">
      <div style={{ width: '160px', minHeight: '2px' }}>
        <Divider variant="regular" />
      </div>
    </ExampleItem>
    <ExampleItem label="Thin (0.5px)" gap="medium" labelSize="medium">
      <div style={{ width: '160px', minHeight: '2px' }}>
        <Divider variant="thin" />
      </div>
    </ExampleItem>
  </ExampleRow>
</ExampleContainer>

```tsx
import { Divider } from '@design-system/divider';

export function Section() {
  return (
    <>
      <Divider variant="regular" />
      {/* ... */}
      <Divider variant="thin" />
    </>
  );
}
```

## Storybook

### Playground

<StorybookIframe storyId="components-divider--playground" showControls={true} height="400px" />

### Visual Matrix

Для просмотра комбинаций вариант × ориентация используйте story **Visual Matrix** в Storybook.

<StorybookIframe storyId="components-divider--visual-matrix" showControls={false} height="400px" />

## Props

[//]: DOCUMENTATION_SECTION_START
[//]: THIS_SECTION_IS_AUTOGENERATED_PLEASE_DONT_EDIT_IT
### DividerProps
| name | type | default value | description |
|------|------|---------------|-------------|
| variant | enum DividerVariant: `"regular"`, `"thin"` | regular | Вариант толщины линии (regular: 1px, thin: 0.5px). По умолчанию: regular |
| orientation | enum DividerOrientation: `"horizontal"`, `"vertical"` | horizontal | Ориентация: горизонтальная или вертикальная. По умолчанию: horizontal |
| className | `string` | - | CSS-класс |


[//]: DOCUMENTATION_SECTION_END

## Accessibility

- **role="separator"** — разделитель корректно объявляется скринридерам
- **aria-orientation** — передаётся ориентация (horizontal/vertical) для контекста
- Компонент принимает стандартные HTML-атрибуты и поддержку `data-test-id` через WithSupportProps

## Best practices

1. **Горизонтальный разделитель** — используйте между блоками контента (секции, параграфы, карточки в колонке).
2. **Вертикальный разделитель** — используйте внутри flex-рядов (тулбары, списки действий), обязательно задайте высоту контейнера (например, `height` или `alignItems: 'stretch'`).
3. **Regular vs Thin** — regular для явного разделения секций, thin — когда нужно визуально ослабить границу (вложенные блоки, плотные списки).
4. **Не перегружайте интерфейс** — не ставьте разделители между каждым элементом; группируйте логические блоки.
