---
title: QuestionTooltip
package: '@ds/tooltip'
description: Специализированный тултип с готовым триггером-иконкой «?» — для подсказок к полям форм и терминам.
order: 2
---

import { QuestionTooltip } from '@ds/tooltip'
import { QuestionTooltipDemo } from '../demos/QuestionTooltipDemo'
import { Question } from '../demos/examples/Question'
import QuestionSrc from '../demos/examples/Question.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import tooltipDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# QuestionTooltip

Тултип с встроенным триггером — иконкой «?» размером 16px. Использует `Tooltip` под капотом, добавляя стандартный triggerButton с `aria-label`.

## Демо

<QuestionTooltipDemo client:visible />

## Когда использовать

- Рядом с названием поля формы, чтобы объяснить назначение поля.
- Рядом с термином или аббревиатурой в тексте и таблицах.
- В онбординговых состояниях — пояснение новой фичи.

## Установка

```bash
pnpm add @ds/tooltip
```

```ts
import { QuestionTooltip } from '@ds/tooltip'
```

## Примеры использования

<Example
  title='Подсказка к полю формы'
  description='aria-label триггера задаётся через triggerLabel'
  code={QuestionSrc}
>
  <Question client:visible />
</Example>

## Props

<PropsTable data={tooltipDoc.QuestionTooltip} />

## Storybook

<StorybookEmbed storyId='components-tooltip-questiontooltip--playground' height={320} />

## Анатомия

### Size
Размер триггера-иконки и тултипа: `xs` — для плотных форм, `s` — дефолт рядом с заголовками секций.

## Figma
<FigmaEmbed node={figmaNode('tooltip')} height={480} title='QuestionTooltip в Figma (Snack UI Kit)' />
