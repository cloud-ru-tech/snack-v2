---
title: Tooltip
package: '@ds/tooltip'
description: Всплывающая подсказка над произвольным триггером — четыре placement, четыре варианта trigger, поддержка клавиатуры и screen-reader.
order: 1
---

import { Tooltip } from '@ds/tooltip'
import { TooltipDemo } from '../demos/TooltipDemo'
import { Basic } from '../demos/examples/Basic'
import BasicSrc from '../demos/examples/Basic.tsx?raw'
import { ClickTrigger } from '../demos/examples/ClickTrigger'
import ClickTriggerSrc from '../demos/examples/ClickTrigger.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import tooltipDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# Tooltip

Pop-up-подсказка над произвольным триггером. Принимает `children` (сам триггер) и `tip` (содержимое подсказки).

## Демо

<TooltipDemo client:visible />

## Когда использовать

- Для раскрытия контекста на icon-only кнопке («Настройки», «Экспорт»).
- Для подсказки по disabled-элементу — почему он недоступен.
- Для аббревиатур и терминов в таблицах.

Когда **не** нужен: для длинного описания — вместо этого Popover или отдельная страница.

## Установка

```bash
pnpm add @ds/tooltip
```

```ts
import { Tooltip } from '@ds/tooltip'
```

## Примеры использования

<Example
  title='1. Базовая подсказка'
  description='Hover + focus по умолчанию'
  code={BasicSrc}
>
  <Basic client:visible />
</Example>

<Example
  title='2. Click trigger'
  description='Открывается по клику и остаётся видимой'
  code={ClickTriggerSrc}
>
  <ClickTrigger client:visible />
</Example>

## Props

<PropsTable data={tooltipDoc.Tooltip} />

## Storybook

<StorybookEmbed storyId='components-tooltip-tooltip--playground' height={320} />

## Анатомия

### Size
`xs` — короткие подписи на одной строке, `s` — многострочные подсказки и описания.

## Figma
<FigmaEmbed node={figmaNode('tooltip')} height={480} title='Tooltip в Figma (Snack UI Kit)' />
