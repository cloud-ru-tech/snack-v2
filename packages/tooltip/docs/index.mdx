---
title: Tooltip
package: '@ds/tooltip'
description: Пакет подсказок — компонент Tooltip для произвольного триггера и QuestionTooltip с иконкой «?» для полей форм и сложной терминологии.
order: 0
---

# Tooltip

Пакет `@ds/tooltip` содержит всплывающие подсказки:

- [**Tooltip**](/components/tooltip/tooltip) — универсальная подсказка. Оборачивает произвольный триггер (кнопка, иконка, текст) и показывает pop-up по hover, focus или клику.
- [**QuestionTooltip**](/components/tooltip/question-tooltip) — специализированный вариант с готовым триггером «?» для полей форм и терминов.

## Состав пакета

| Компонент | Когда использовать |
|-----------|---------------------|
| `Tooltip` | Любой кастомный триггер |
| `QuestionTooltip` | Иконка «?» рядом с полем формы или термином |

## Установка

```bash
pnpm add @ds/tooltip
```

```ts
import { Tooltip, QuestionTooltip } from '@ds/tooltip'
import '@ds/tooltip/style.css'
```

## Общие принципы

- **Короткий текст.** Tooltip не заменяет документацию — 1–2 предложения максимум.
- **Не прячьте критичную информацию.** Если действие блокируется по причине, покажите её в UI, а не только в tooltip.
- **Достижимо с клавиатуры.** По умолчанию trigger `hoverAndFocusVisible` — подсказка появляется при focus через Tab.
- **Уважайте `prefers-reduced-motion`.** Анимации появления отключаются системно.
