/**
 * Stories-level test-id'ы пакета `@ds/bottom-sheet` (multi-component).
 * High-level `BottomSheet` пока держит свои id в `BottomSheet/testIds.ts`.
 */
export const TEST_IDS = {
  bottomSheetCustom: {
    root: 'bottom-sheet-custom',
    triggerOpen: 'bottom-sheet-custom-trigger',
  },
} as const;
