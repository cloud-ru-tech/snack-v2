---
title: Tag
package: '@ds/tag'
description: Пакет компактных меток — компонент Tag (опционально удаляемый или ссылочный) и контейнер TagRow для групп меток с обрезанием по строкам.
order: 0
---

# Tag

Пакет `@ds/tag` содержит компоненты для отображения коротких меток, категорий и статусов:

- [**Tag**](/components/tag/tag) — одиночная метка. Рендерится как `<span>` по умолчанию, как `<a>` при передаче `href`, и опционально отображает кнопку удаления через `onDelete`.
- [**TagRow**](/components/tag/tag-row) — контейнер группы меток. Поддерживает ограничение по количеству видимых строк с кнопкой «+N ещё».

## Состав пакета

| Компонент | Когда использовать |
|-----------|---------------------|
| `Tag` | Одиночная метка — категория записи, статус, фильтр |
| `TagRow` | Несколько меток рядом, особенно если их количество переменное |

## Установка

```bash
pnpm add @ds/tag
```

```ts
import { Tag, TagRow } from '@ds/tag'
import '@ds/tag/style.css'
```

## Общие принципы

- **Appearance — семантика, не декор.** `red` для ошибок, `green` для успеха, `blue` для информации. Не раскрашивайте метки ради разнообразия.
- **Размер — от контекста.** `xs` — в таблицах и плотных списках, `s` — в карточках, `m` — когда метка сама по себе элемент UI.
- **Удаляемые метки — только там, где есть состояние.** `onDelete` применяется в форме с выбранными фильтрами, не в read-only списках.
- **Ссылочные метки — навигация по категории.** `href` превращает `<Tag>` в ссылку, которая ведёт на страницу фильтра.
