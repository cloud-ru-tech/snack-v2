/**
 * Stories-level data-test-id для field-decorator. Один источник истины для stories
 * (Playground/VisualMatrix) и для E2E helpers.
 */
export const TEST_IDS = {
  label: { root: 'label' },
  hint: { root: 'hint' },
  fieldDecorator: { root: 'field-decorator' },
} as const;
