---
title: Link
description: Textual navigation element with variants and underline control.
order: 10
version: '0.1.0'
---

import { Link } from '../src';

Use Link for inline navigation and contextual actions. It supports visual variants, weight, underline behavior, and automatic external attributes.

**Version:** `{frontmatter.version}`

## Changelog

<>
  {/* Note: Import path will be transformed during sync
Original: ../../../../apps/docs/src/components/Changelog.astro
After sync: ../../../../components/Changelog.astro */}
</>

import Changelog from '../../../../apps/docs/src/components/Changelog.astro';

<Changelog packageName="link" />

## Live examples

<div style={{ display: 'grid', gap: 12 }}>
  <Link href="#">Primary link</Link>
  <Link href="#" variant="muted">
    Muted link
  </Link>
  <Link href="#" variant="ghost">
    Ghost link
  </Link>
  <Link href="https://example.com" isExternal>
    External link
  </Link>
  <Link href="#" underline="always">
    Always underline
  </Link>
  <Link href="#" underline="none" weight="semibold">
    No underline, semibold
  </Link>
</div>

## Storybook

<>
  {/* Note: Import path will be transformed during sync
Original: ../../../../apps/docs/src/components/StorybookIframe.astro
After sync: ../../../../components/StorybookIframe.astro */}
</>

import StorybookIframe from '../../../../apps/docs/src/components/StorybookIframe.astro';

<StorybookIframe storyId="components-link--basic" height="320px" />

## Usage

```tsx
import { Link } from '@design-system/link';

export function Example() {
  return (
    <p>
      Go to{' '}
      <Link href="https://example.com" isExternal>
        example.com
      </Link>
    </p>
  );
}
```

## Variants

- `primary` — brand accent
- `muted` — subdued text links
- `ghost` — neutral text

## Underline

- `hover` (default) — underline on hover/focus
- `always` — underline always visible
- `none` — no underline

## Props

- `variant` (`primary | muted | ghost`)
- `weight` (`regular | semibold`)
- `underline` (`hover | always | none`)
- `isExternal` (`boolean`) — adds `target="_blank" rel="noopener noreferrer"`
- All native anchor attributes
