---
title: FileUpload
package: '@ds/dropzone'
description: Обёртка над произвольным триггером — открывает нативный диалог выбора файлов по клику. Без визуальной зоны.
order: 52
---

import { FileUpload } from '@ds/dropzone'
import { FileUploadBasic } from '../demos/examples/FileUploadBasic'
import FileUploadBasicSrc from '../demos/examples/FileUploadBasic.tsx?raw'
import { FileUploadFormField } from '../demos/examples/FileUploadFormField'
import FileUploadFormFieldSrc from '../demos/examples/FileUploadFormField.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import dropzoneDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# FileUpload

Невизуальная обёртка над произвольным триггером — как правило, кнопкой. Клик по триггеру открывает системный диалог выбора файлов. Используйте, когда загрузка — вспомогательное действие в форме или toolbar, и не нужна отдельная зона перетаскивания.

## Когда использовать
- Кнопка «Загрузить файлы» рядом с другими полями формы.
- Иконка-загрузка в toolbar.
- Скрытый триггер, когда загрузка инициируется программно через клик на дочерний элемент.

Когда **не** подходит: если нужна явная зона drag-n-drop — используйте [Dropzone](/components/dropzone/dropzone) или [HiddenDropZone](/components/dropzone/hidden-drop-zone).

## Анатомия

### Upload mode
`single` — один файл за раз, повторный выбор заменяет предыдущий; `multiple` — батч-загрузка, файлы накапливаются.

### Поле формы
`FileUpload` проксирует нативные атрибуты `<input type="file">` на скрытый input — компонент работает как полноценное поле формы:

- `name`, `id`, `required`, `capture`, `form` — стандартные атрибуты; `id` связывает input с `<label htmlFor>`.
- `innerRef` — ссылка на нативный input (для интеграции с react-hook-form: `ref` из `register(name)`).
- `onChange` — нативный `onChange` input, вызывается до валидации с исходным событием (точка интеграции с form-библиотеками).
- `disabled` — деактивирует триггер и input.

### Валидация
Ограничения `accept` (MIME-тип, шаблон `image/*` или расширение `.pdf`; строка или массив) и `maxSize` (байты) разбивают выбранные файлы на две группы:

- принятые уходят в `onFilesUpload(files)`;
- отклонённые — в `onFilesReject(rejections)` с причиной `maxSize` или `mime`.

Размер проверяется раньше типа: превысивший лимит файл отклоняется с причиной `maxSize`.

## Установка
```bash
pnpm add @ds/dropzone
```

```ts
import { FileUpload } from '@ds/dropzone'
```

## Примеры использования
<Example
  title='Кнопка-триггер'
  description='FileUpload оборачивает Button'
  code={FileUploadBasicSrc}
>
  <FileUploadBasic client:visible />
</Example>

<Example
  title='Поле формы с валидацией'
  description='name/id/required, accept + maxSize, отказ через onFilesReject'
  code={FileUploadFormFieldSrc}
>
  <FileUploadFormField client:visible />
</Example>

## Props
<PropsTable data={dropzoneDoc.FileUpload} />

## Storybook
<StorybookEmbed storyId='components-dropzone-fileupload--playground' height={320} />

## Figma
<FigmaEmbed node={figmaNode('dropzone')} height={480} title='FileUpload в Figma (Snack UI Kit)' />
