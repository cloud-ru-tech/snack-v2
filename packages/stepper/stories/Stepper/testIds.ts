export const TEST_IDS = {
  root: 'stepper',
  next: 'stepper-next',
  prev: 'stepper-prev',
  /** Корень wrapper'а examples-stories (BasicFlow, Mobile, …) — для визуального
   * кадрирования вокруг всей композиции (Stepper + buttons). */
  example: 'stepper-example',
  adaptive: 'stepper-adaptive',
  mobile: 'stepper-mobile',
} as const;
