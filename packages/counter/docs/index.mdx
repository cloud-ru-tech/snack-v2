---
title: Counter
package: '@ds/counter'
description: Компактный счётчик для отображения числовых меток, бейджей и состояний.
order: 2
---

import { CounterDemo } from '../demos/CounterDemo'
import { Basic } from '../demos/examples/Basic'
import BasicSrc from '../demos/examples/Basic.tsx?raw'
import { PlusThreshold } from '../demos/examples/PlusThreshold'
import PlusThresholdSrc from '../demos/examples/PlusThreshold.tsx?raw'
import { ThousandsK } from '../demos/examples/ThousandsK'
import ThousandsKSrc from '../demos/examples/ThousandsK.tsx?raw'
import { Red } from '../demos/examples/Red'
import RedSrc from '../demos/examples/Red.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import counterDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# Counter

Счётчик — компактный компонент для отображения числовых значений внутри другой UI-поверхности: кнопок, тегов, пунктов меню, навигации.

## Демо
<CounterDemo client:visible />

## Когда использовать
- Количество непрочитанных уведомлений у иконки колокольчика.
- Количество элементов в корзине или списке.
- Бейдж на табе / пункте меню.

Когда **не** нужен `Counter`: для крупных числовых метрик используйте `Typography`, для статусов — `Tag`.

## Анатомия

### Appearance (default `primary`)
Цветовая схема счётчика. Значение совпадает с именем семантической палитры темы:

- `primary` — акцентный (непрочитанные, новые).
- `neutral` — нейтральный.
- `red` — ошибки, срочные элементы.
- `orange` — предупреждения.
- `yellow` — внимание.
- `green` — успех, положительный статус.
- `blue` — информационный.
- `violet` — дополнительный акцент.
- `pink` — дополнительный акцент.

### Variant
Формат отображения числа: `count` — число как есть, `count-plus` — сокращение с плюсом после лимита (`10+`, `99+`), `count-k` — сокращение в тысячах (`1K`, `12K`).

### Size
Размер бейджа: `xs` — на иконках/мелких элементах, `s` — дефолт в табах и пунктах меню.

### Role appearance (default `accent`)
Роль, в которой применяется выбранный `appearance`:

- `accent` — насыщенная заливка, основной токен appearance.
- `decor` — декоративная подложка, визуально более мягкий бейдж.

## Установка
```bash
pnpm add @ds/counter
```

```ts
import { Counter, APPEARANCE, VARIANT, SIZE } from '@ds/counter'
```

## Примеры использования
<Example title='Обычное число' code={BasicSrc}>
  <Basic client:visible />
</Example>

<Example title='С порогом 10+' code={PlusThresholdSrc}>
  <PlusThreshold client:visible />
</Example>

<Example title='Тысячи как K' code={ThousandsKSrc}>
  <ThousandsK client:visible />
</Example>

<Example title='Red — ошибки и срочные элементы' code={RedSrc}>
  <Red client:visible />
</Example>

## Props
<PropsTable data={counterDoc.Counter} />

## Storybook
<StorybookEmbed storyId='components-counter--playground' height={360} />

## Figma
<FigmaEmbed node={figmaNode('counter')} height={480} title='Counter в Figma (Snack UI Kit)' />
