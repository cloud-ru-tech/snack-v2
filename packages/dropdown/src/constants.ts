export const STATE = {
  Loading: 'loading',
  NotFound: 'not-found',
  NoData: 'no-data',
  DataError: 'data-error',
} as const;

export const TEST_IDS = {
  root: 'dropdown',
  trigger: 'dropdown-trigger',
} as const;
