---
title: NotificationPanel
package: '@ds/uikit-product-notification'
description: Адаптивная обёртка панели уведомлений — на desktop открывает NotificationPanelContent в drawer, на mobile в bottom-sheet (выбор по layoutType).
order: 130
---

import { PanelScenario } from '../demos/examples/PanelScenario'
import PanelScenarioSrc from '../demos/examples/PanelScenario.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'
import notificationDoc from './props.json'

# NotificationPanel

Адаптивная обёртка [`NotificationPanelContent`](/components/uikit-product-notification/notification-panel). На desktop открывает панель в drawer, на mobile — в bottom-sheet. Выбор раскладки — по `layoutType` (как у `Widget` / `Toolbar`).

Контент передаётся пропом `content` как элемент `<NotificationPanelContent />`, а не как пропсы.

## Когда использовать

- Лента уведомлений, открываемая из иконки в шапке или тулбаре.
- Один и тот же контент панели на desktop и mobile без дублирования разметки.

## Анатомия

### Layout type (default `desktop`)

Тип раскладки определяет обёртку:

- `desktop` — drawer (Figma `notificationDrawer`).
- `mobile` — bottom-sheet (Figma `notificationBottomSheet`).

### Position (default `right`)

Сторона, с которой выезжает drawer. Только для `desktop`.

### Width (default `s`)

Ширина drawer. Только для `desktop`.

## Установка

```bash
pnpm add @ds/uikit-product-notification
```

```ts
import { NotificationPanelContent, NotificationPanel } from '@ds/uikit-product-notification'
```

## Примеры использования

<Example
  title='Открытие из кнопки в шапке'
  description='Триггер со счётчиком непрочитанных, useState для open/close, контент — <NotificationPanelContent /> с живыми карточками. Порталу нужен PortalContextProvider.'
  code={PanelScenarioSrc}
>
  <PanelScenario client:visible />
</Example>

## Props

<PropsTable data={notificationDoc.NotificationPanel} />

## Storybook

<StorybookEmbed storyId='uikit-product-notification-notificationpanel--playground' height={520} />

## Figma

<FigmaEmbed node={figmaNode('uikit-product-notification', 'notification-drawer')} />

<FigmaEmbed node={figmaNode('uikit-product-notification', 'notification-bottom-sheet')} />
