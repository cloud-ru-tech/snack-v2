---
title: FieldText
package: '@ds/fields'
description: Однострочное текстовое поле с поддержкой префиксов/постфиксов, иконок-слотов и кнопок очистки/копирования.
order: 31
---

import { Basic } from '../demos/examples/Basic'
import BasicSrc from '../demos/examples/Basic.tsx?raw'
import { Affixes } from '../demos/examples/Affixes'
import AffixesSrc from '../demos/examples/Affixes.tsx?raw'
import { WithError } from '../demos/examples/WithError'
import WithErrorSrc from '../demos/examples/WithError.tsx?raw'
import { Readonly } from '../demos/examples/Readonly'
import ReadonlySrc from '../demos/examples/Readonly.tsx?raw'
import { IconBefore } from '../demos/examples/IconBefore'
import IconBeforeSrc from '../demos/examples/IconBefore.tsx?raw'
import { IconAfter } from '../demos/examples/IconAfter'
import IconAfterSrc from '../demos/examples/IconAfter.tsx?raw'
import { IconAndAffix } from '../demos/examples/IconAndAffix'
import IconAndAffixSrc from '../demos/examples/IconAndAffix.tsx?raw'
import { ElementBefore } from '../demos/examples/ElementBefore'
import ElementBeforeSrc from '../demos/examples/ElementBefore.tsx?raw'
import { ElementAfter } from '../demos/examples/ElementAfter'
import ElementAfterSrc from '../demos/examples/ElementAfter.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'
import fieldsDoc from './props.json'

# FieldText

Однострочный текстовый input. Базируется на `FieldDecorator` и `@ds/input-private`. Поддерживает текстовые префикс/постфикс, иконки слева/справа, кнопки очистки и копирования (последняя — только в `readonly`).

## Когда использовать

- Стандартное поле в формах, фильтрах, редакторах настроек.
- Отображение значений только-для-чтения (`readonly` + автоматическая кнопка копирования).
- Для специфичного поведения поиска (suggest, recent) используйте `@ds/search`, не FieldText.

## Анатомия

### Size (default `m`)

| Значение | Когда |
|----------|-------|
| `s` | Плотные таблицы, inline-редактирование |
| `m` | Стандартные формы (по умолчанию) |
| `l` | Лендинги, primary-формы |

### ValidationState (default `default`)

Управляет тонировкой acrylic-фона поля и иконкой подсказки. Проп `error` форсит `error`.

| Значение | Когда |
|----------|-------|
| `default` | Нет валидации — нейтральный фон |
| `error` | Поле не прошло валидацию — красная тонировка фона |
| `warning` | Предупреждение, ввод допустим — жёлтая тонировка фона |
| `success` | Подтверждение успешного ввода — зелёная тонировка фона |
| `valid` | Нейтральный baseline: текст подсказки `textTertiary`, без цвета и иконки валидации — мягкое подтверждение, не акцентирует внимание. Не путать с `success` (единственный «зелёный» state) |

### Background (default `true`)

Acrylic-фон поля. При `background = false` поле остаётся без подложки (borderless-вид) — уместно во встроенных/inline-сценариях, где фон даёт окружающий контейнер.

### Outline (default `true`)

Разделитель (`Divider`) между основным полем и слотами `elementBefore` / `elementAfter`. `outline = false` убирает вертикальную линию — слот и поле визуально сливаются.

### Кнопки очистки и копирования

Постфикс-слот несёт взаимоисключающие кнопки:

- Кнопка очистки (`showClearButton`, по умолчанию `true`) появляется при непустом `value` в редактируемом поле. В `disabled` и `readonly` она скрыта — очищать нечего.
- Кнопка копирования (`showCopyButton`, по умолчанию `true`) появляется только при `readonly` и непустом `value`. После клика иконка на 2 секунды меняется на галочку — подтверждение, что значение скопировано.

## Установка

```bash
pnpm add @ds/fields
```

```ts
import { FieldText } from '@ds/fields'
```

## Примеры использования

<Example title='Базовое поле' description='Controlled FieldText с label и hint.' code={BasicSrc}>
  <Basic client:visible />
</Example>

<Example title='Префикс и постфикс' description='Текстовые префикс/постфикс по краям инпута.' code={AffixesSrc}>
  <Affixes client:visible />
</Example>

<Example title='Иконка слева' description='iconBefore — визуальный маркер контекста (поиск, дата, валюта).' code={IconBeforeSrc}>
  <IconBefore client:visible />
</Example>

<Example title='Иконка справа' description='iconAfter — обычно подсказка/help (тултип не входит в FieldText, оборачивайте отдельно).' code={IconAfterSrc}>
  <IconAfter client:visible />
</Example>

<Example title='Иконка + префикс/постфикс' description='Слоты iconBefore + prefix + postfix комбинируются — иконка идёт первой, текст — между ней и инпутом.' code={IconAndAffixSrc}>
  <IconAndAffix client:visible />
</Example>

<Example title='Кнопка-слот слева' description='elementBefore — split-кнопка слева (например, выбор даты, лупа поиска с действием).' code={ElementBeforeSrc}>
  <ElementBefore client:visible />
</Example>

<Example title='Кнопка-слот справа' description='elementAfter — split-кнопка справа (выбор валюты, единицы измерения, dropdown).' code={ElementAfterSrc}>
  <ElementAfter client:visible />
</Example>

<Example title='Валидация ошибки' description='Проп error форсит validationState=error и показывает иконку, если showHintIcon.' code={WithErrorSrc}>
  <WithError client:visible />
</Example>

<Example title='Readonly с копированием' description='Readonly FieldText показывает кнопку копирования значения в буфер. После копирования иконка на 2 секунды меняется на галочку — подтверждение, что значение скопировано.' code={ReadonlySrc}>
  <Readonly client:visible />
</Example>

## Props

<PropsTable data={fieldsDoc.FieldText} />

## Storybook

<StorybookEmbed storyId='components-fields-fieldtext--playground' height={420} />

## Figma

<FigmaEmbed node={figmaNode('fields', 'field-text')} />
