---
title: Search
package: '@ds/search'
description: Компонент поискового поля — три размера, фон, разделитель, состояния loading и disabled, опциональный слот для действия.
order: 0
---

import { Basic } from '../demos/examples/Basic'
import BasicSrc from '../demos/examples/Basic.tsx?raw'
import { SizeL } from '../demos/examples/SizeL'
import SizeLSrc from '../demos/examples/SizeL.tsx?raw'
import { Loading } from '../demos/examples/Loading'
import LoadingSrc from '../demos/examples/Loading.tsx?raw'
import { Disabled } from '../demos/examples/Disabled'
import DisabledSrc from '../demos/examples/Disabled.tsx?raw'
import { TransparentBackground } from '../demos/examples/TransparentBackground'
import TransparentBackgroundSrc from '../demos/examples/TransparentBackground.tsx?raw'
import { Example } from '#docs/components/Example'
import { PropsTable } from '#docs/components/PropsTable'
import { StorybookEmbed } from '#docs/components/StorybookEmbed'
import searchDoc from './props.json'
import { FigmaEmbed } from '#docs/components/FigmaEmbed'
import { figmaNode } from '#docs/lib/figma'

# Search

Поисковое поле дизайн-системы: `<input type='search'>` с иконкой, опциональной кнопкой очистки, состоянием `loading` и слотом `buttonField` справа для дополнительной кнопки-действия («Найти», «Фильтр»).

## Когда использовать
- Основной поиск по списку / каталогу / таблице.
- Header-поиск с моментальной отдачей (live search).
- Форма поиска с явной кнопкой «Найти» (`buttonField`).

Когда **не** подходит: для выбора из фиксированного списка используйте `Combobox`, для фильтра с префиксом — `Input` + `Chip`.

## Установка
```bash
pnpm add @ds/search
```

```ts
import { Search } from '@ds/search'
```

## Примеры использования
<Example title='Базовое поле' code={BasicSrc}>
  <Basic client:visible />
</Example>

<Example title='Размер l для hero' code={SizeLSrc}>
  <SizeL client:visible />
</Example>

<Example title='Loading' code={LoadingSrc}>
  <Loading client:visible />
</Example>

<Example title='Disabled' code={DisabledSrc}>
  <Disabled client:visible />
</Example>

<Example title='Без фона' code={TransparentBackgroundSrc}>
  <TransparentBackground client:visible />
</Example>

## Props
<PropsTable data={searchDoc.Search} />

## Storybook
<StorybookEmbed storyId='components-search--playground' height={240} />

## Анатомия

### Size
Высота поля: `s` — компактный (списки, тулбары), `m` — дефолт, `l` — для крупных форм и посадочных страниц.

## Figma
<FigmaEmbed node={figmaNode('search')} height={480} title='Search в Figma (Snack UI Kit)' />
