---
description: Греп-скан пакета на ревью-ниты A–L + чек-листы по доменам — когда просят проверить пакет перед MR, прогнать pre-mr аудит, «что зацепит ревьюер»; финал миграции/имплементации.
argument-hint: <pkg-name>
---

Следуй инструкции из `.claude/skills/pre-mr-audit.md` в этом репо. Полный текст скилла (греп-блоки A–L + консолидированные чек-листы по доменам, anti-patterns) — там; здесь только регистрация как slash-команды.

Перед стартом прочитай `.claude/skills/pre-mr-audit.md` целиком через `Read`. Подставь `PKG` из аргумента (имя папки в `packages/`).
