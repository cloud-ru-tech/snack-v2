/**
 * External URLs surfaced in the docs portal (header buttons, MDX links, etc.).
 * Centralized so a repo move or channel change is a one-line edit.
 */
export const EXTERNAL_LINKS = {
  /** Source repository (GitLab tree). */
  repo: 'https://git.sbercloud.tech/sbercloud-ui/tokens-design-system/variables/storybook',
  /** Mattermost support channel. */
  support: 'https://mm.cloud.ru/default/channels/ff-support',
} as const;
