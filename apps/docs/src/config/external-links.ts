/**
 * External URLs surfaced in the docs portal (header buttons, MDX links, etc.).
 * Centralized so a repo move is a one-line edit.
 */
export const EXTERNAL_LINKS = {
  /** Source repository (GitHub). */
  repo: 'https://github.com/cloud-ru-tech/snack-v2',
} as const;
