---
/**
 * LlmLink component - displays a link to component-specific LLM.txt file
 * 
 * Usage in MDX:
 * <LlmLink component="avatar" />
 */

interface Props {
  component: string;
}

const { component } = Astro.props;

// Get base URL - use relative path for production flexibility
const llmUrl = `/_llms-txt/components/llm-${component}.txt`;
const fullUrl = import.meta.env.SITE ? 
  `${import.meta.env.SITE.replace(/\/+$/, '')}${llmUrl}` : 
  `http://localhost:4321${llmUrl}`;
---

<div class="llm-link-container">
  <div class="llm-link-header">
    <svg
      class="llm-icon-large"
      xmlns="http://www.w3.org/2000/svg"
      width="20"
      height="20"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
      <polyline points="14 2 14 8 20 8"></polyline>
      <line x1="16" y1="13" x2="8" y2="13"></line>
      <line x1="16" y1="17" x2="8" y2="17"></line>
      <polyline points="10 9 9 9 8 9"></polyline>
    </svg>
    <strong>LLM-оптимизированная документация</strong>
  </div>
  
  <p class="llm-description">
    Документация этого компонента доступна в текстовом формате для AI-ассистентов и языковых моделей:
  </p>
  
  <div class="llm-link-row">
    <a href={llmUrl} class="llm-link" target="_blank" rel="noopener noreferrer">
      <svg
        class="llm-icon"
        xmlns="http://www.w3.org/2000/svg"
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
        <polyline points="15 3 21 3 21 9"></polyline>
        <line x1="10" y1="14" x2="21" y2="3"></line>
      </svg>
      <span>Открыть llm-{component}.txt</span>
    </a>
    
    <button class="copy-button" data-url={fullUrl} title="Скопировать ссылку">
      <svg
        class="copy-icon"
        xmlns="http://www.w3.org/2000/svg"
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
      </svg>
    </button>
  </div>
  
  <div class="llm-url">
    <code>{llmUrl}</code>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const copyButtons = document.querySelectorAll('.copy-button');
    
    copyButtons.forEach(button => {
      button.addEventListener('click', async () => {
        const url = button.getAttribute('data-url');
        if (!url) return;
        
        try {
          await navigator.clipboard.writeText(url);
          
          // Visual feedback
          const originalHTML = button.innerHTML;
          button.innerHTML = `
            <svg
              class="copy-icon"
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
          `;
          button.classList.add('copied');
          
          setTimeout(() => {
            button.innerHTML = originalHTML;
            button.classList.remove('copied');
          }, 2000);
        } catch (err) {
          console.error('Failed to copy:', err);
        }
      });
    });
  });
</script>

<style>
  .llm-link-container {
    margin: 1.5rem 0;
    padding: 1.25rem;
    background: var(--sl-color-gray-6);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 0.5rem;
    border-left: 4px solid var(--sl-color-text-accent);
  }

  .llm-link-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
    color: var(--sl-color-text);
  }

  .llm-icon-large {
    flex-shrink: 0;
    width: 20px;
    height: 20px;
    color: var(--sl-color-text-accent);
  }

  .llm-description {
    margin: 0 0 1rem 0;
    color: var(--sl-color-gray-2);
    font-size: 0.95rem;
    line-height: 1.5;
  }

  .llm-link-row {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .llm-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--sl-color-text-accent);
    text-decoration: none;
    font-weight: 500;
    padding: 0.5rem 0.75rem;
    background: var(--sl-color-gray-5);
    border-radius: 0.375rem;
    transition: all 0.2s ease;
    flex: 1;
  }

  .llm-link:hover {
    background: var(--sl-color-gray-4);
    text-decoration: none;
    transform: translateX(2px);
  }

  .llm-icon {
    flex-shrink: 0;
    width: 16px;
    height: 16px;
  }

  .copy-button {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.5rem;
    background: var(--sl-color-gray-5);
    border: 1px solid var(--sl-color-gray-4);
    border-radius: 0.375rem;
    cursor: pointer;
    transition: all 0.2s ease;
    color: var(--sl-color-text);
  }

  .copy-button:hover {
    background: var(--sl-color-gray-4);
    border-color: var(--sl-color-text-accent);
  }

  .copy-button.copied {
    background: var(--sl-color-green);
    border-color: var(--sl-color-green);
    color: white;
  }

  .copy-icon {
    width: 16px;
    height: 16px;
  }

  .llm-url {
    padding: 0.5rem 0.75rem;
    background: var(--sl-color-gray-7);
    border-radius: 0.25rem;
    overflow-x: auto;
  }

  .llm-url code {
    font-size: 0.85rem;
    font-family: var(--sl-font-mono);
    color: var(--sl-color-text);
    background: none;
    padding: 0;
  }

  :global([data-theme='light']) .llm-link-container {
    background: var(--sl-color-bg-inline-code);
    border-color: var(--sl-color-hairline-light);
  }

  :global([data-theme='dark']) .llm-link-container {
    background: var(--sl-color-bg-inline-code);
    border-color: var(--sl-color-hairline);
  }

  :global([data-theme='light']) .llm-url {
    background: var(--sl-color-gray-6);
  }
</style>
